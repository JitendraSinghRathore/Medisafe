<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="BottomNavigation">
        <item name="android:background">@color/colorPrimaryDark</item>
        <item name="itemBackground">@drawable/splash</item>
        <item name="itemIconTint">@color/colorPrimary</item>
        <item name="itemTextColor">@color/bgBottomNavigation</item>
        <item name="paddingStart">@dimen/fab_margin</item>
        <item name="paddingEnd">@dimen/fab_margin</item>
    </style>

    <!-- Tab Layout All title-text-in-lower-case use this style in tabviewLayout -->
    <style name="CustomTextAppearanceTab" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:textSize">11sp</item>
        <item name="android:textAllCaps">false</item>
    </style>


</resources>
